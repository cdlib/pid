<section id="user_registration">
	<h3>Registration</h3>
	
	<div id="register" class="navy">
		<a href="/user/list">User List</a>
	</div>
	
	<form action="/user/register" method="post">
		<% if @super %>
			<label>Is a system administrator?</label><input type="checkbox" name="super" id="super" <% if @params[:super] %>checked="checked"<% end %> />
		<% end %>
		<label>Group:</label>
		<% if @groups %>
			<select name="group" id="group">
				<% @groups.each do |group| %>
				<option value="<%= group.id %>" <% if params[:group] == group.id %>selected="selected"<% end %>><%= group.name %></option>
				<% end %>
			</select>
		<% else %>
			<input type="text" readonly="true" value="<%= @params[:group] %>" />
		<% end %>
		<label>Can maintain group?</label><input type="checkbox" name="maintainer" id="maintainer" <% if @params[:maintainer] %>checked="checked"<% end %> />
		<label>User Id:</label><input type="text" name="login" id="login" value="<%= @params[:userid] %>" />
		<label>Full Name:</label><input type="text" name="name" id="name" value="<%= @params[:name] %>" />
		<label>Email:</label><input type="email" name="email" id="email" value="<%= @params[:email] %>" />
		<label>Affiliation:</label><input type="text" name="affiliation" id="affiliation" value="<%= @params[:affiliation] %>" />
		<label>Password:</label><input type="password" name="password" id="password" />
		<label>Confirm:</label><input type="password" name="confirm" id="confirm" />
		<input type="submit" id="submit" name="submit" value="Submit" />
	</form>
	
	<div class="errors">
		<%= @msg %>
		
    <% if @user && @user.errors %>
      <% @user.errors.full_messages.each do | message | %>
        <%= message %></br>
      <% end %>
    <% end %>
	</div>
</section>
